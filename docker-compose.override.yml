# Optional services — uncomment to enable.
# This file merges automatically with docker-compose.yml.
#
# Scene captioning via Ollama + Gemma 3:
#   1. Uncomment the ollama service below
#   2. docker compose up -d
#   3. docker compose exec ollama ollama pull gemma3:4b
#   4. Set CAPTIONER_ENABLED=true in .env
#
# services:
#   ollama:
#     container_name: ring-ollama
#     image: ollama/ollama:latest
#     restart: unless-stopped
#     ports:
#       - "11434:11434"
#     volumes:
#       - ollama_data:/root/.ollama
#     deploy:
#       resources:
#         reservations:
#           devices:
#             - driver: nvidia
#               count: 1
#               capabilities: [gpu]
#
# volumes:
#   ollama_data:
